import styled from 'styled-components';

import { MapContrastText } from '@/client/styles/utils';

export const Title = styled.h1`
  font-size: 2rem;
  font-weight: 600;
  letter-spacing: 1.1px;
  margin-bottom: 0;
  color: ${MapContrastText};
`;
